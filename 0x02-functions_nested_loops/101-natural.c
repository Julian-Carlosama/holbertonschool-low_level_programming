#include <stdio.h>

/**
 * main - Prints naturals numbers below 1024 that are
 * multiplies 3 or 5
 *
 * Return: Always 0.
 */
int main(void)
{
	int x = 0, y = 0;

	for (x = 1; i < 1024; x++)
	{
		if ((x % 3) == 0 || (a % 5) == 0)
			y += x;
	}
	printf("%d\n", y);
	return (0);
}
