#include "holberton.h"

/**
 * _strlen - Returns the leng of the string
 *
 * @s: Is the string to process
 *
 * Return: return the string long
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; *(s + i) != 0; i++)
	{
	}

	return (i);
}
